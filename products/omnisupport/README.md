# OmniSupport

Мультитенантная SaaS-платформа для омниканальной поддержки клиентов с AI-ассистентом.

## 🎯 Описание

OmniSupport — это современная платформа для организации клиентской поддержки, которая объединяет:

- **Омниканальность** — интеграция с Telegram, WhatsApp, веб-виджетом и другими каналами
- **AI-ассистент** — автоматические ответы на базе RAG (Retrieval-Augmented Generation) с поддержкой YandexGPT и GigaChat
- **Мультитенантность** — изолированные рабочие пространства для разных компаний
- **Аналитика** — дашборды с метриками качества поддержки
- **База знаний** — загрузка документов (PDF, DOCX, HTML) для обучения AI

## 📁 Структура проекта

```
omnisupport/
├── backend/                 # Python backend (FastAPI)
│   ├── services/            # Микросервисы
│   │   ├── core/            # Основной API (тикеты, пользователи)
│   │   ├── admin/           # Панель администратора тенанта
│   │   ├── ai/              # AI-сервис (RAG, LLM)
│   │   ├── channel/         # Обработка каналов связи
│   │   └── superadmin/      # Управление платформой
│   ├── workers/             # Фоновые задачи (ARQ)
│   │   ├── ai.py            # AI-обработка сообщений
│   │   ├── analytics.py     # Сбор аналитики
│   │   ├── notification.py  # Уведомления
│   │   ├── router.py        # Маршрутизация сообщений
│   │   └── webhook.py       # Обработка вебхуков
│   ├── shared/              # Общий код
│   │   ├── models/          # SQLAlchemy модели
│   │   ├── schemas/         # Pydantic схемы
│   │   ├── auth/            # JWT аутентификация
│   │   ├── events/          # Event-driven архитектура
│   │   ├── config.py        # Конфигурация
│   │   ├── database.py      # Подключение к БД
│   │   └── storage.py       # S3 хранилище файлов
│   ├── alembic/             # Миграции БД
│   ├── docker-compose.yml   # Инфраструктура
│   └── pyproject.toml       # Зависимости Python
├── widget/                  # Встраиваемый виджет чата (Vue/React + Vite)
│   ├── src/                 # Исходный код виджета
│   └── sdk/                 # SDK для интеграции
├── frontend/                # Панель управления (в разработке)
└── wireframes/              # Дизайн-макеты
```

## 🚀 Быстрый старт

### Требования

- Docker & Docker Compose
- Python 3.11+ (для локальной разработки)
- Node.js 18+ (для виджета)

### Запуск через Docker Compose

```bash
cd backend

# Скопировать конфигурацию
cp .env.example .env

# Запустить все сервисы
docker-compose up -d

# Проверить статус
docker-compose ps
```

Сервисы будут доступны:
- **Core API**: http://localhost:8000
- **PostgreSQL**: localhost:5432
- **Redis**: localhost:6379
- **Qdrant**: http://localhost:6333

## ⚙️ Переменные окружения

### Backend (.env)

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| **База данных** | | |
| `DATABASE_URL` | PostgreSQL connection string | `postgresql+asyncpg://omnisupport:omnisupport_dev@localhost:5432/omnisupport` |
| **Redis** | | |
| `REDIS_URL` | Redis connection string | `redis://localhost:6379/0` |
| **Qdrant** | | |
| `QDRANT_URL` | Qdrant HTTP URL | `http://localhost:6333` |
| `QDRANT_HOST` | Qdrant host | `localhost` |
| `QDRANT_PORT` | Qdrant port | `6333` |
| **JWT** | | |
| `JWT_SECRET_KEY` | Секретный ключ для JWT | ⚠️ Обязательно сменить! |
| `JWT_ALGORITHM` | Алгоритм подписи | `HS256` |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Время жизни access токена | `15` |
| `REFRESH_TOKEN_EXPIRE_DAYS` | Время жизни refresh токена | `7` |
| **Приложение** | | |
| `DEBUG` | Режим отладки | `true` |
| `ENVIRONMENT` | Окружение | `development` |
| `API_V1_PREFIX` | Префикс API | `/api/v1` |
| `CORS_ORIGINS` | Разрешённые origins | `["http://localhost:3000"]` |
| **LLM провайдеры** (опционально) | | |
| `YANDEX_GPT_API_KEY` | API ключ YandexGPT | — |
| `YANDEX_GPT_FOLDER_ID` | Folder ID в Yandex Cloud | — |
| `GIGACHAT_CLIENT_ID` | Client ID GigaChat | — |
| `GIGACHAT_CLIENT_SECRET` | Client Secret GigaChat | — |
| **Каналы** (опционально) | | |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | — |
| `WHATSAPP_API_URL` | URL WhatsApp API | — |
| `WHATSAPP_API_TOKEN` | Токен WhatsApp API | — |
| **S3 хранилище** (опционально) | | |
| `S3_ENDPOINT_URL` | URL S3-совместимого хранилища | — |
| `S3_ACCESS_KEY_ID` | Access Key ID | — |
| `S3_SECRET_ACCESS_KEY` | Secret Access Key | — |
| `S3_BUCKET_NAME` | Имя бакета | `omnisupport` |
| **Email** (опционально) | | |
| `SMTP_HOST` | SMTP сервер | `localhost` |
| `SMTP_PORT` | SMTP порт | `587` |
| `SMTP_USER` | SMTP пользователь | — |
| `SMTP_PASSWORD` | SMTP пароль | — |
| `SMTP_FROM` | Адрес отправителя | `noreply@example.com` |
| `SMTP_TLS` | Использовать TLS | `false` |

### Widget (.env)

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VITE_API_URL` | URL backend API | `http://localhost:8000` |
| `VITE_WIDGET_URL` | URL виджета (production) | `https://widget.omnisupport.ru` |

## 🗃️ Миграции базы данных

Проект использует [Alembic](https://alembic.sqlalchemy.org/) для миграций.

```bash
cd backend

# Применить все миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1

# Создать новую миграцию
alembic revision --autogenerate -m "описание изменений"

# Показать текущую версию
alembic current

# Показать историю миграций
alembic history
```

### В Docker

```bash
# Применить миграции в контейнере
docker-compose exec core alembic upgrade head

# Создать миграцию
docker-compose exec core alembic revision --autogenerate -m "описание"
```

## 🛠️ Разработка

### Локальный запуск backend

```bash
cd backend

# Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate   # Windows

# Установить зависимости
pip install -e ".[dev]"

# Запустить инфраструктуру
docker-compose up -d postgres redis qdrant

# Применить миграции
alembic upgrade head

# Запустить API
uvicorn services.core.main:app --reload

# В отдельном терминале — воркеры
arq workers.main.WorkerSettings
```

### Виджет

```bash
cd widget

npm install
npm run dev
```

## 📊 Технологический стек

**Backend:**
- FastAPI — веб-фреймворк
- SQLAlchemy 2.0 — ORM (async)
- PostgreSQL — основная БД
- Redis — кэш, очереди, pub/sub
- Qdrant — векторная БД для RAG
- ARQ — фоновые задачи
- Alembic — миграции

**Widget:**
- Vite — сборка
- TailwindCSS — стили

**Инфраструктура:**
- Docker & Docker Compose
- Prometheus — метрики

## 📝 Лицензия

Проприетарное ПО. Все права защищены.
